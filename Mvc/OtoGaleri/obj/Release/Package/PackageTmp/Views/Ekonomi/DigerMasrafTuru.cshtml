@model IEnumerable<OtoGaleri_Entities.Tablolar.DigerMasrafTurleri>
@{
    ViewBag.Title = "DigerMasrafTuru";
}
<title>Diğer Masraflar Türü</title>
<div class="row">
    <div class="col-md-10 col-md-offset-1">
        <h2>Diğer Masraf Türleri</h2>
        <hr />
    </div>

</div>
<div class="row">
    <div class="col-md-10 col-md-offset-1">
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.MasrafAdi)
                </th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.MasrafAdi)
                    </td>

                </tr>
            }

        </table>
        @Html.ActionLink("Diğer Masrafları Dön", "DigerMasraflar", "Ekonomi", null, new { @class = "btn btn-default" })
        @Html.ActionLink("Diğer Masraf Türü Ekle", "DigerMasrafTuruEkle", "Ekonomi", null, new { @class = "btn btn-primary" })

    </div>
</div>

